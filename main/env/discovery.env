# Eureka registry (HTTP). JVM apps on the host use http://localhost:${DISCOVERY_PUBLISH_PORT:-8761}/eureka/
SERVER_PORT=8761
# Hostname used in Eureka server metadata (optional for standalone).
EUREKA_HOST=discovery
