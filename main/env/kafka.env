# Host clients (Spring apps running on your machine) use localhost:KAFKA_HOST_PUBLISH_PORT.
# Containers on the streaming network must use kafka:9092 inside KAFKA_BOOTSTRAP_SERVERS.
KAFKA_HOST_PUBLISH_PORT=9094
ALLOW_PLAINTEXT_LISTENER=yes
BITNAMI_DEBUG=false
